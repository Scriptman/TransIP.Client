namespace TransIP.Client.Enums
{
    public enum AdditionalData
    {
        // Important is to keep in mind that we use these for bitwaise stuff, so use 1,2,4,8,16,32 etc.
        Nameservers = 1,
        Contacts = 2
    }
}
